#include "non_existent_file.h" // File header non esistente

#include <stdio.h> 

#include "missing_quote.h // Direttiva con apici non chiusi alla fine

#include another_malformed.h" // Direttiva con apici non aperti all'inizio

int main() {
    // Codice di esempio
    return 0;
}