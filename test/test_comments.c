// Questo e' un commento su una riga singola

/* Questo e' un
   commento su
   piu' righe */

   int main() {
    int a = 10; /* Commento in linea */ // Un altro commento sulla stessa riga
    int b = 20; // Solo commento dopo il codice
    /* Commento blocco che inizia e finisce sulla stessa riga */ int c = 30;

    /* Commento blocco aperto alla fine del file...